export type UpdateCardData = {
  id: string;
  title: string;
  subtitle: string;
  tone: string;
};

export type VideoCardData = {
  id: string;
  title: string;
  subtitle: string;
  channel: string;
  duration: string;
  views: string;
  age: string;
  tag: string;
  gradient: string;
};
